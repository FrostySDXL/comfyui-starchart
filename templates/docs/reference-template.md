<!-- Remove placeholder text that does not apply. Keep the page factual, source-backed, and lightweight. -->

# Page Title

**Evidence:** Source-backed from pinned snapshots
**Last Updated:** YYYY-MM-DD
**Primary Source:** ComfyUI core `vX.Y.Z` `server.py` (pinned snapshot)

## Scope

State exactly what this page documents and what it does not attempt to cover.

## Who This Page Is For

Name the reader only if the audience is not obvious from the title.

## Primary Sources

- `references/snapshots/YYYY-MM-DD/comfyui-core-vX.Y.Z/server.py` (vX.Y.Z, commit SHA)
- Add every pinned source file or official docs page needed to support the claims on this page.

## API Surface

Document concrete items (fields, methods, endpoints) in the order they appear
in the source. Use declarative tone.

For each item, prefer a small repeated structure such as:

- `Item name`
  - What it is
  - Inputs or fields
  - Returns, output shape, or side effects
  - Constraints, version notes, or gaps when evidence is incomplete

## Read Next

- Link only to the next one or two pages that most readers should open next.
